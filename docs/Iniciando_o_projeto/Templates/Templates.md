# Templates no Django

Os **templates** são responsáveis pela camada de **apresentação** da aplicação Django. Eles definem como os dados enviados pelas *views* serão exibidos ao usuário, mantendo a separação entre **lógica** e **interface**.

O Django utiliza o **Django Template Language (DTL)**, uma linguagem própria para renderização de HTML dinâmico de forma segura e organizada.

## Conteúdos

1. [DTL Syntax](DTL_Syntax.md)
   Introdução à sintaxe do Django Template Language, abordando variáveis, tags, filtros e estruturas de controle utilizadas no dia a dia.

2. [Template Inheritance](Template_Inheritance/Template_Inheritance.md)
   Explica o funcionamento da herança de templates, uso de `base.html`, blocos reutilizáveis e organização de layouts.

